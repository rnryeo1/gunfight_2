# Cloudflare Worker + 정적 자산 (Vite dist). 배포: npm run build 후 wrangler deploy
# GitHub Actions 가 main 푸시 시 자동으로 wrangler deploy → workers.dev 갱신
# https://developers.cloudflare.com/workers/static-assets/

name = "gunfight2"
compatibility_date = "2024-12-30"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
