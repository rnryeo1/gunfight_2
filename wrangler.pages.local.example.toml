# 이 레포는 루트에 Pages 전용 `wrangler.toml` 을 둡니다 (Git + CLI 공통).
# 프로젝트 이름이 다르면 `name = "…"` 만 바꾸면 됩니다.
# Workers용 `main` 키는 넣지 마세요.

name = "gun-fight"
compatibility_date = "2024-12-30"
pages_build_output_dir = "dist"
