# 이 레포는 기본이 Cloudflare Worker + [assets] (wrangler.toml) 입니다.
# Pages 전용으로만 쓰려면 Cloudflare 문서의 Pages + wrangler 설정을 따로 두세요.

name = "gunfight2"
compatibility_date = "2024-12-30"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
